{% extends 'base.html.twig' %}

{% block body %}
<h1>Книги</h1>
{% include 'default/menu.html.twig' %}

    {% for book in books %}
        <h2><a href="{{ url('edit_book', {id:book.id}) }}">{{ book.name }}</a></h2>
    {% endfor %}
{% endblock %}